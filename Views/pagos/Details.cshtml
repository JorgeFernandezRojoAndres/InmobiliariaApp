@model InmobiliariaApp.Models.Pago

@{
    ViewData["Title"] = "Detalles del Pago";
}

<h2 class="mb-4">💵 Detalles del Pago</h2>

<div class="card shadow-sm">
    <div class="card-body">
        <h4 class="card-title">Pago #@Model.Id</h4>
        <hr />

        <p><strong>Contrato:</strong> @Model.ContratoDescripcion</p>
        <p><strong>Fecha:</strong> @Model.FechaPago.ToShortDateString()</p>
        <p><strong>Detalle:</strong> @Model.Detalle</p>
        <p><strong>Importe:</strong> @Model.Importe.ToString("C")</p>

        <!-- 🔹 Sección de Auditoría: solo visible para administradores -->
        @if (User.IsInRole("Administrador"))
        {
            <hr />
            <h5>🛡️ Auditoría</h5>
            <dl class="row">
                <dt class="col-sm-3">Creado por</dt>
                <dd class="col-sm-9">
                    @(Model.UsuarioCreador != null 
                        ? $"{Model.UsuarioCreador.Nombre} {Model.UsuarioCreador.Apellido}" 
                        : $"Usuario ID {Model.CreadoPor}")
                </dd>

                @if (Model.AnuladoPor.HasValue)
                {
                    <dt class="col-sm-3">Anulado por</dt>
                    <dd class="col-sm-9">
                        @(Model.UsuarioAnulador != null 
                            ? $"{Model.UsuarioAnulador.Nombre} {Model.UsuarioAnulador.Apellido}" 
                            : $"Usuario ID {Model.AnuladoPor}")
                    </dd>
                }
            </dl>
        }
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">✏️ Editar</a>
    <a asp-action="Index" class="btn btn-secondary">⬅️ Volver</a>
</div>
